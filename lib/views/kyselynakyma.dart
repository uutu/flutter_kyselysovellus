import 'package:flutter/material.dart';

class Kyselynakyma extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Text('Hei maailma!')));
  }
}
